

export default function Preview({ userData }) {
  return (
    <>
      {console.log('Preview', userData)}
      {/* <div>Fullname: { (userInfo.lastName || '') + ' ' + (userInfo.firstName || '') }</div>
      <div>Email: { userInfo.email || '' }</div>
      <div>Phone: { userInfo.phone || '' }</div> */}
    </>
  )
}